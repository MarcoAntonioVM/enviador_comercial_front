import { EntityFormPage } from "@/components/entity/EntityFormPage";
import { templateFormConfig } from "../template.form";

export default function TemplateFormPage() {
  return <EntityFormPage config={templateFormConfig} />;
}
